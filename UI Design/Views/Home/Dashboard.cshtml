<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShopSmart Dashboard</title>

    <!-- Importing Montserrat font from Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600&display=swap" rel="stylesheet">

    <style>
        /* background image */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-image: url('https://images.unsplash.com/photo-1498837167922-ddd27525d352?fm=jpg&q=60&w=3000&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxleHBsb3JlLWZlZWR8NXx8fGVufDB8fHx8fA%3D%3D');
            background-size: cover;
            background-position: center center;
            background-attachment: fixed;
            color: #333;
        }

        /* Header styling */
        header {
            padding: 15px 20px; /* Increased padding to make the header taller */
            background-color: #111;
            color: white;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: sticky;
            top: 0;
            z-index: 5;
            height: 80px; /* Increase height for better spacing */
        }

        /* Sidebar styling */
        .sidebar {
            height: 100%;
            width: 0; /* Sidebar is initially closed */
            position: fixed;
            top: 0;
            left: 0;
            background-color: #111;
            overflow-x: hidden;
            padding-top: 60px;
            z-index: 10;
            transition: 0.3s;
        }

            .sidebar a {
                padding: 15px;
                text-decoration: none;
                font-size: 18px;
                color: #818181;
                display: block;
                transition: 0.3s;
            }

                .sidebar a:hover {
                    color: #f1f1f1;
                }

            .sidebar .closebtn {
                position: absolute;
                top: 20px;
                right: 25px;
                font-size: 36px;
                color: white;
                cursor: pointer;
            }

        /* Toggle button styling */
        .toggle-sidebar {
            font-size: 20px;
            cursor: pointer;
            color: white;
            background: none;
            border: none;
            padding: 8px;
            position: absolute;
            left: 20px;
        }

            .toggle-sidebar:hover {
                color: #888;
            }

        /* Auth logo styling (positioned above the black bar at the top-right) */
        .auth-logo {
            height: 40px; /* Adjust logo size */
            cursor: pointer;
            position: absolute;
            top: -20px; /* Move the logo above the header */
            right: 20px; /* Distance from the right side */
            z-index: 15; /* Ensure the logo is above other elements */
        }

        /* Dropdown Menu styling */
        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #111;
            min-width: 160px;
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
            z-index: 1;
            border-radius: 5px;
        }

            .dropdown-content a {
                color: white;
                padding: 12px 16px;
                text-decoration: none;
                display: block;
                text-align: left;
            }

                .dropdown-content a:hover {
                    background-color: #444;
                }

        /* Dropdown hover effect */
        .dropdown:hover .dropdown-content {
            display: block;
        }

        /* Main dashboard content */
        .main-content {
            margin-left: 0; /* Adjust the margin so content doesn't overlap sidebar when closed */
            padding: 20px;
        }

        /* Updated header with text shadow */
        .dashboard-header {
            font-size: 2rem;
            color: #fff; /* White text color */
            margin-bottom: 20px;
            text-align: center;
            text-shadow: 3px 3px 5px rgb(0, 0, 0), 0 0 25px rgb(0, 0, 0), 0 0 5px rgb(0, 0, 0); /* Black shadow */
        }

        /* Dashboard cards */
        .card-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
            margin-top: 500px; /* Move cards further down significantly */
        }

        .card {
            background-color: rgba(255, 255, 255, 0.8); /* Slight transparency */
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 250px;
            text-align: center;
            transition: transform 0.2s ease;
        }

            .card:hover {
                transform: scale(1.05);
            }

            .card h3 {
                font-size: 1.2rem;
                color: #555;
                margin-bottom: 10px;
            }

            .card p {
                color: #777;
                font-size: 1rem;
            }

            .card button {
                padding: 8px 20px;
                background-color: #333;
                color: white;
                border: none;
                border-radius: 5px;
                cursor: pointer;
                transition: background-color 0.3s ease;
            }

                .card button:hover {
                    background-color: #444;
                }
    </style>
</head>

<body>

    <!-- Sidebar navigation -->
    <div id="sidebar" class="sidebar">
        <span class="closebtn" onclick="closeSidebar()">×</span>
        <a href="#">Dashboard</a>
        <a href="#">Create List</a>
        <a href="#">Saved Lists</a>
        <a href="#">Popular Products</a>
    </div>

    <!-- Header with toggle button and auth logo -->
    <header>
        <button class="toggle-sidebar" onclick="openSidebar()">☰</button>
    </header>

    <!-- Auth Logo placed above the header -->
    <div class="dropdown">
        <img src="your-logo-url.png" alt="ShopSmart Logo" class="auth-logo" onclick="toggleDropdown()">
        <div id="dropdownMenu" class="dropdown-content">
            <a href="#" onclick="createAccount()">Create Account</a>
            <a href="#" onclick="login()">Log In</a>
            <a href="#" onclick="manageAccount()">Manage Account</a>
        </div>
    </div>

    <!-- Main dashboard content -->
    <div class="main-content">
        <div class="dashboard-header">Welcome to ShopSmart</div>

        <!-- Dashboard Cards -->
        <div class="card-container">
            <div class="card">
                <h3>Recent List</h3>
                <p>No recent lists available.</p>
                <button onclick="createNewList()">Create New List</button>
            </div>
            <div class="card">
                <h3>Saved Lists</h3>
                <p>View your saved lists.</p>
                <button onclick="viewSavedLists()">View Lists</button>
            </div>
            <div class="card">
                <h3>Popular Products</h3>
                <p>See trending products.</p>
                <button onclick="viewPopularProducts()">View Products</button>
            </div>
        </div>
    </div>

    <script>
        // Ensure sidebar is closed on page load
        window.onload = function () {
            document.getElementById("sidebar").style.width = "0";
        };

        function createNewList() {
            alert("Redirecting to list creation...");
        }

        function viewSavedLists() {
            alert("Viewing saved

   